# 統計検定準1級section11～16

---

- [統計検定準1級section11～16](#統計検定準1級section1116)
  - [11 検定（2級範囲）](#11-検定2級範囲)
    - [11.1 母平均の検定](#111-母平均の検定)
      - [11.1.1 母平均の検定（母分散既知）の場合](#1111-母平均の検定母分散既知の場合)
        - [何これ：すべての基本。ばらつきの正体が分かっている理想の世界](#何これすべての基本ばらつきの正体が分かっている理想の世界)
      - [11.1.2 母平均の検定（母分散未知，小標本）の場合](#1112-母平均の検定母分散未知小標本の場合)
        - [何これ：現実世界のリアルな検定（データが少ないとき）](#何これ現実世界のリアルな検定データが少ないとき)
      - [11.1.3 母平均の検定（母分散未知，大標本）の場合](#1113-母平均の検定母分散未知大標本の場合)
        - [何これ：データが大量にあるなら、細かいことは気にせず正規分布でOK](#何これデータが大量にあるなら細かいことは気にせず正規分布でok)
    - [11.2 母分散の検定](#112-母分散の検定)
        - [何これ：「データのばらつき加減（キレの良さ）」が想定通りかをチェックする](#何これデータのばらつき加減キレの良さが想定通りかをチェックする)
    - [11.3 母比率の検定と母比率の差の検定](#113-母比率の検定と母比率の差の検定)
      - [11.3.1 母比率の検定（大標本）の場合](#1131-母比率の検定大標本の場合)
        - [何これ：「コインの表が出る確率は本当に50%か？」を確かめる](#何これコインの表が出る確率は本当に50かを確かめる)
      - [11.3.2 母比率の差の検定（大標本）の場合](#1132-母比率の差の検定大標本の場合)
        - [何これ：「新薬の有効率」と「旧薬の有効率」に、本当の差があるかを比べる](#何これ新薬の有効率と旧薬の有効率に本当の差があるかを比べる)
    - [11.4 母平均の差の検定](#114-母平均の差の検定)
      - [11.4.1 対応のある2群の場合](#1141-対応のある2群の場合)
        - [何これ：同じ人の「ダイエット前」と「ダイエット後」を比べる](#何これ同じ人のダイエット前とダイエット後を比べる)
      - [11.4.2 対応のない2群で，母分散既知の場合](#1142-対応のない2群で母分散既知の場合)
        - [何これ：完全に赤の他人である「A組」と「B組」のテスト平均を比べる（理想環境）](#何これ完全に赤の他人であるa組とb組のテスト平均を比べる理想環境)
      - [11.4.3 対応のない2群で，母分散未知（大標本）の場合](#1143-対応のない2群で母分散未知大標本の場合)
        - [何これ：赤の他人の2群を比べる（データが大量にある現実的な環境）](#何これ赤の他人の2群を比べるデータが大量にある現実的な環境)
      - [11.4.4 対応のない2群で，母分散未知（小標本，等分散）の場合](#1144-対応のない2群で母分散未知小標本等分散の場合)
        - [何これ：「ばらつき方は同じ」と信じて、2つの少ないデータを合体させて戦うt検定](#何こればらつき方は同じと信じて2つの少ないデータを合体させて戦うt検定)
    - [11.5 等分散仮説の検定](#115-等分散仮説の検定)
        - [何これ：前の項（11.4.4）の前提である「等分散」が本当に正しいか調べる検定](#何これ前の項1144の前提である等分散が本当に正しいか調べる検定)
    - [11.6 適合度検定](#116-適合度検定)
        - [何これ：「このサイコロ、本当にすべての目が1/6の確率で出る？」を確かめる](#何これこのサイコロ本当にすべての目が16の確率で出るを確かめる)
    - [11.7 独立性の検定](#117-独立性の検定)
        - [何これ：「性別」と「支持政党」に関係があるかを調べる](#何これ性別と支持政党に関係があるかを調べる)
  - [12 検定（準1級範囲）](#12-検定準1級範囲)
    - [12.1 ポアソン分布に関する検定](#121-ポアソン分布に関する検定)
        - [何これ：めったに起きない事故の回数（ポアソン）も、たくさん集まれば正規分布の仲間入り](#何これめったに起きない事故の回数ポアソンもたくさん集まれば正規分布の仲間入り)
    - [12.2 適合度検定](#122-適合度検定)
        - [何これ：理論値の計算に「データからの推測」が混ざると、その分だけ自由度が減る（-l）](#何これ理論値の計算にデータからの推測が混ざるとその分だけ自由度が減る-l)
    - [12.3 尤度比検定](#123-尤度比検定)
        - [何これ：仮説検定の「すべてのボス」。どんなに複雑なモデルでも比較できる万能天秤](#何これ仮説検定のすべてのボスどんなに複雑なモデルでも比較できる万能天秤)
  - [13 ノンパラメトリック法](#13-ノンパラメトリック法)
        - [何これ：データの「形」が分からなくても、順番や符号だけで戦うサバイバル統計学](#何これデータの形が分からなくても順番や符号だけで戦うサバイバル統計学)
    - [13.1 符号検定](#131-符号検定)
        - [何これ：大きさは無視！「勝ち」か「負け」の数だけで多数決をとる](#何これ大きさは無視勝ちか負けの数だけで多数決をとる)
    - [13.2 符号付き順位検定](#132-符号付き順位検定)
        - [何これ：符号検定に「どれくらい大きく勝ったか（順位）」の情報もプラスした贅沢版](#何これ符号検定にどれくらい大きく勝ったか順位の情報もプラスした贅沢版)
    - [13.3 ウィルコクソンの順位和検定](#133-ウィルコクソンの順位和検定)
        - [何これ：2つのグループを全員混ぜてランキングを作り、チーム対抗の「順位の合計点」で競う](#何これ2つのグループを全員混ぜてランキングを作りチーム対抗の順位の合計点で競う)
    - [13.4 並べ替え検定](#134-並べ替え検定)
        - [何これ：コンピュータの力で「あり得る全パターン」をシャッフルしてゴリ押しする検定](#何これコンピュータの力であり得る全パターンをシャッフルしてゴリ押しする検定)
    - [13.5 クラスカル・ウォリス検定](#135-クラスカルウォリス検定)
        - [何これ：一元配置分散分析（ANOVA）のノンパラ版。3チーム以上の順位戦](#何これ一元配置分散分析anovaのノンパラ版3チーム以上の順位戦)
    - [13.6 順位相関係数](#136-順位相関係数)
        - [何これ：とんでもない大富豪（外れ値）が混ざっていても騙されない、タフな相関関係](#何これとんでもない大富豪外れ値が混ざっていても騙されないタフな相関関係)
  - [14 マルコフ連鎖](#14-マルコフ連鎖)
        - [何これ：「過去の履歴」は一切関係ない。明日の天気が決まるのは「今日の天気」だけという世界](#何これ過去の履歴は一切関係ない明日の天気が決まるのは今日の天気だけという世界)
    - [14.1 状態確率ベクトル](#141-状態確率ベクトル)
        - [何これ：現時点で「どの状態に何%の確率で滞在しているか」の割り当て表](#何これ現時点でどの状態に何の確率で滞在しているかの割り当て表)
    - [14.2 推移確率行列](#142-推移確率行列)
        - [何これ：状態を変化させる「サイコロのルール一覧表」](#何これ状態を変化させるサイコロのルール一覧表)
    - [14.3 定常分布](#143-定常分布)
        - [何これ：何回推移を繰り返しても、もう確率の割合が変化しなくなる「究極の黄金バランス」](#何これ何回推移を繰り返してももう確率の割合が変化しなくなる究極の黄金バランス)
    - [14.4 パラメータの推定](#144-パラメータの推定)
        - [何これ：過去の足跡（時系列データ）をバラバラに分解して、一番しっくりくるルール（θ）を逆算する](#何これ過去の足跡時系列データをバラバラに分解して一番しっくりくるルールθを逆算する)
  - [15 確率過程の基礎](#15-確率過程の基礎)
    - [15.1 確率過程](#151-確率過程)
        - [何これ：時間とともに変化していく「確率の連続ドラマ」](#何これ時間とともに変化していく確率の連続ドラマ)
    - [15.2 ブラウン運動](#152-ブラウン運動)
        - [何これ：過去を一切引きずらず、常にサイコロを振ってフラフラと彷徨う究極のランダム](#何これ過去を一切引きずらず常にサイコロを振ってフラフラと彷徨う究極のランダム)
    - [15.3 ポアソン過程](#153-ポアソン過程)
        - [何これ：「いつ起きるか分からない出来事」を、ひたすらカチカチと数え続けるカウンター](#何これいつ起きるか分からない出来事をひたすらカチカチと数え続けるカウンター)
    - [15.4 複合ポアソン過程](#154-複合ポアソン過程)
        - [何これ：「お客さんが来るか？」に加えて、「いくらお金を落としていくか？」も掛け合わせた現実モデル](#何これお客さんが来るかに加えていくらお金を落としていくかも掛け合わせた現実モデル)
  - [16 重回帰分析](#16-重回帰分析)
        - [何これ：「いくつかの原因（$x$）」から、「1つの結果（$y$）」を予想する最強の方程式](#何これいくつかの原因xから1つの結果yを予想する最強の方程式)
    - [16.1 最小2乗法](#161-最小2乗法)
        - [何これ：データと予測線の「ズレ」が一番小さくなるように、直線をピタッと合わせる職人技](#何これデータと予測線のズレが一番小さくなるように直線をピタッと合わせる職人技)
    - [16.2 決定係数](#162-決定係数)
        - [何これ：「この予測方程式は、現実を何%くらい説明できているか？」の成績表](#何これこの予測方程式は現実を何くらい説明できているかの成績表)
    - [16.3 自由度調整済み決定係数](#163-自由度調整済み決定係数)
        - [何これ：無駄なおまけ（変数）を増やすとペナルティを食らう、厳しめの実力判定シート](#何これ無駄なおまけ変数を増やすとペナルティを食らう厳しめの実力判定シート)
    - [16.4 回帰係数の推定と検定](#164-回帰係数の推定と検定)
        - [何これ：その原因（変数）は、本当に結果に影響を与えているか裁判にかける](#何これその原因変数は本当に結果に影響を与えているか裁判にかける)
    - [16.5 一般化最小2乗法](#165-一般化最小2乗法)
        - [何これ：データのばらつきが不平等な時に、エコひいきを無くすために平らにならす技](#何これデータのばらつきが不平等な時にエコひいきを無くすために平らにならす技)
    - [16.6 正則化](#166-正則化)
        - [何これ：過去問（手元のデータ）を丸暗記しすぎて、本番の試験（未来の予測）で大爆死するのを防ぐ](#何これ過去問手元のデータを丸暗記しすぎて本番の試験未来の予測で大爆死するのを防ぐ)
    - [16.7 L2正則化（リッジ回帰）](#167-l2正則化リッジ回帰)
        - [何これ：みんなで少しずつ遠慮し合って、極端に尖ったボスを作らない平和主義モデル](#何これみんなで少しずつ遠慮し合って極端に尖ったボスを作らない平和主義モデル)
    - [16.8 L1正則化（Lasso回帰）](#168-l1正則化lasso回帰)
        - [何これ：役に立たない変数を容赦なく「クビ（完全にゼロ）」にする、断捨離の鬼モデル](#何これ役に立たない変数を容赦なくクビ完全にゼロにする断捨離の鬼モデル)
    - [16.9 正則化パス](#169-正則化パス)
        - [何これ：縛りをキツくしていった時に、誰が最後まで生き残るかの「サバイバル軌跡チャート」](#何これ縛りをキツくしていった時に誰が最後まで生き残るかのサバイバル軌跡チャート)
    - [16.10 Elastic Net](#1610-elastic-net)
        - [何これ：リッジとラッソのいいとこ取り。仲良しグループを一緒に採用するハイブリッド](#何これリッジとラッソのいいとこ取り仲良しグループを一緒に採用するハイブリッド)
    - [16.11 Fused lasso](#1611-fused-lasso)
        - [何これ：「お隣さん同士は、きっと同じような値のはず」という隣人縛り](#何これお隣さん同士はきっと同じような値のはずという隣人縛り)


---

https://toketarou.com/quasi_1_cheatsheet/

---

## 11 検定（2級範囲）
### 11.1 母平均の検定
#### 11.1.1 母平均の検定（母分散既知）の場合
母集団分布が母分散 $\sigma^2$ が既知の正規分布で標本の大きさが $n$ のとき，母平均が $\mu$ に等しいという帰無仮説のもとで，次の検定統計量が標準正規分布に従うことを利用します。
$$
Z = \dfrac{\overline{X}-\mu}{\sigma/\sqrt{n}} \sim N(0, 1)
$$
ここから、標準正規分布表とかで有意水準の区間を調べて不等式で挟んで検定する。
以下の検定も同様の流れなので、何の分布に従っているかのみをまとめていく。

##### 何これ：すべての基本。ばらつきの正体が分かっている理想の世界
全体のばらつき（$\sigma$）があらかじめ分かっている状態で、「手元のデータの平均値（$\overline{X}$）が、想定している平均値（$\mu$）からどれくらい離れているか」を測ります。すべての検定の原点となる形です。

#### 11.1.2 母平均の検定（母分散未知，小標本）の場合
母集団分布が正規分布で標本の大きさが $n$ のとき，母平均が $\mu$ に等しいという帰無仮説のもとで，次の検定統計量が自由度 $n-1$ の $t$ 分布に従うことを利用します。
$$
T = \dfrac{\overline{X}-\mu}{\sqrt{U^2/n}} \sim t(n-1)
$$

##### 何これ：現実世界のリアルな検定（データが少ないとき）
本当のばらつきが分からないので、手元のデータから計算した「不偏分散 $U^2$」で代用します。代用品を使うことで生まれる「自信のなさ（不確実性）」を考慮して、正規分布よりも少し裾の広い（大外れが出やすい）**$t$分布**でジャッジします。

#### 11.1.3 母平均の検定（母分散未知，大標本）の場合
標本の大きさを $n$ として，母平均が $\mu$ に等しいという帰無仮説のもとで次の検定統計量が近似的に標準正規分布に従うことを利用します。この場合，中心極限定理により，母集団分布はどんなものでもかまいません。
$$
Z = \dfrac{\overline{X}-\mu}{\sqrt{U^2/n}} \sim N(0, 1)
$$

##### 何これ：データが大量にあるなら、細かいことは気にせず正規分布でOK
データ数 $n$ が十分に大きければ、中心極限定理のパワーによって、元のデータがどんな形（サイコロの目、歪んだ分布など）であっても、平均値のばらつきは綺麗な**標準正規分布**に収束します。代用品の不偏分散 $U^2$ も、ほぼ真の分散とみなしてよくなります。

---

### 11.2 母分散の検定
母集団分布が平均が未知の正規分布で標本の大きさが $n$ のとき，母分散が $\sigma^2$ に等しいという帰無仮説のもとで，次の検定統計量が自由度 $n-1$ の $\chi^2$ 分布に従うことを利用します。
$$
V = \dfrac{(n-1)U^2}{\sigma^2} \sim \chi^2(n-1)
$$

##### 何これ：「データのばらつき加減（キレの良さ）」が想定通りかをチェックする
「平均値」ではなく「ばらつきの大きさ（分散）」そのものを検定します。手元のばらつき（$U^2$）が、想定しているばらつき（$\sigma^2$）の何倍になっているかを計算すると、それは**カイ二乗分布**に従います。工場の製品の品質が安定しているか（ブレが大きくないか）を調べる時などに使われます。

---

### 11.3 母比率の検定と母比率の差の検定
#### 11.3.1 母比率の検定（大標本）の場合
母比率が $p$ であるという帰無仮説のもとで，標本の大きさが $n$ のとき，標本比率を $X/n$ として，次の検定統計量が近似的に標準正規分布に従うことを利用します。
$$
Z = \dfrac{\frac{X}{n}-p}{\sqrt{\frac{p(1-p)}{n}}} \sim N(0, 1)
$$

##### 何これ：「コインの表が出る確率は本当に50%か？」を確かめる
「はい / いいえ」で答えるような比率のデータ（二項分布）を扱います。データ数 $n$ が大きければ、比率のばらつきも正規分布で近似できます。分母にある $\sqrt{p(1-p)/n}$ は、比率の世界における「標準偏差」のようなものです。

#### 11.3.2 母比率の差の検定（大標本）の場合

> [!IMPORTANT]
> **🔥 訂正：分母のばらつき（標準誤差）の計算について**
> 元のテキストの数式では、2つの群の標本比率を別々に分母に入れていましたが、帰無仮説は「2つの母比率に差がない（$p_1 = p_2 = p$）」という前提です。
> そのため、検定を行う際は2つの群のデータをがっちゃんこ（プール）して計算した**共通の比率 $\hat{p}$** を分母に使うのが統計学的な王道（かつ2級の標準）になります。数式を以下のように修正します。

$$
Z = \dfrac{\frac{X_1}{n_1} - \frac{X_2}{n_2}}{\sqrt{\hat{p}(1-\hat{p})\left(\frac{1}{n_1} + \frac{1}{n_2}\right)}} \sim N(0, 1) \quad \left(\text{ただし } \hat{p} = \frac{X_1 + X_2}{n_1 + n_2}\right)
$$

##### 何これ：「新薬の有効率」と「旧薬の有効率」に、本当の差があるかを比べる
2つのグループの比率の引き算です。「差がない」という仮説のもとで戦うため、分母では2つのデータを1つにまとめた共通の比率 $\hat{p}$ を使って全体のばらつきを見積もるのが、この検定の最大のポイントです。

---

### 11.4 母平均の差の検定
2群の母平均に差がないという帰無仮説のもとで，それぞれ以下のように計算します。

#### 11.4.1 対応のある2群の場合
母集団分布が正規分布で標本の大きさが $n$（対応するデータが $n$ 組）のとき，差の標本平均を $\overline{X}$，差の不偏分散を $U^2$ とするとき，次の検定統計量が自由度 $n-1$ の $t$ 分布に従うことを利用します。
$$
T = \dfrac{\overline{X}}{\sqrt{U^2/n}} \sim t(n-1)
$$

##### 何これ：同じ人の「ダイエット前」と「ダイエット後」を比べる
同じ対象から取ったペアのデータ（例：同じ人の前後、右足と左足など）の引き算です。2つのグループを別々に考えるのではなく、**「引き算した後の1組のデータ」**として扱えるため、実質的には「11.1.2（1標本のt検定）」と全く同じシンプルな数式に早変わりします。

#### 11.4.2 対応のない2群で，母分散既知の場合
母集団分布がそれぞれ正規分布で，母分散が $\sigma_1^2, \sigma_2^2$，標本の大きさが $n_1, n_2$ のとき，次の検定統計量が標準正規分布に従うことを利用します。
$$
\dfrac{\overline{X}-\overline{Y}}{\sqrt{\frac{\sigma_1^2}{n_1}+\frac{\sigma_2^2}{n_2}}} \sim N(0, 1)
$$

##### 何これ：完全に赤の他人である「A組」と「B組」のテスト平均を比べる（理想環境）
「対応のない2群」とは、別々のグループのことです（例：東京の学生と大阪の学生）。それぞれのグループのばらつき（$\sigma_1^2, \sigma_2^2$）があらかじめ分かっている場合の、引き算の正規分布です。

#### 11.4.3 対応のない2群で，母分散未知（大標本）の場合
大標本であれば，不偏分散を $U_1^2, U_2^2$，標本の大きさをそれぞれ $n_1, n_2$ とすると，次の検定統計量が標準正規分布に従うことを利用します。
$$
\dfrac{\overline{X}-\overline{Y}}{\sqrt{\frac{U_1^2}{n_1}+\frac{U_2^2}{n_2}}} \sim N(0, 1)
$$

##### 何これ：赤の他人の2群を比べる（データが大量にある現実的な環境）
実際のばらつきは分かりませんが、データが大量にあるため、それぞれの不偏分散をそのまま使って**標準正規分布**に放り込むことができます。

#### 11.4.4 対応のない2群で，母分散未知（小標本，等分散）の場合
母集団分布がそれぞれ正規分布で，2つの母分散が等しいとき，標本の大きさをそれぞれ $n_1, n_2$ とすると，次の検定統計量が自由度 $n_1+n_2-2$ の $t$ 分布に従うことを利用します。
$$
\dfrac{\overline{X}-\overline{Y}}{\sqrt{\hat{U}^2\left(\frac{1}{n_1}+\frac{1}{n_2} \right)}} \sim t(n_1+n_2-2)\quad\left(\text{where}~~\hat{U}^2=\dfrac{\sum\limits_{k=1}^{n_1}(X_k-\overline{X})^2+\sum\limits_{k=1}^{n_2}(Y_k-\overline{Y})^2}{n_1+n_2-2}\right)
$$

##### 何これ：「ばらつき方は同じ」と信じて、2つの少ないデータを合体させて戦うt検定
データ数が少ないけれど、「2つのグループのばらつきの大きさ（母分散）は同じはずだ」という前提（等分散）がある時の手法です。2つのグループのデータを合わせて、より信頼できる「プールされた不偏分散 $\hat{U}^2$」を作り、それを分母にして**$t$分布**で検定します。

---

### 11.5 等分散仮説の検定
2つの正規母集団の無作為標本（標本の大きさはそれぞれ $n_A, n_B$）から求められる不偏分散をそれぞれ $U_A^2, U_B^2$ とし，これらが独立であるものとします。2群の母分散が等しいという帰無仮説のもとで，次の検定統計量が $F(n_B-1, n_A-1)$ に従うことを利用します。
$$
F = \dfrac{U_B^2}{U_A^2} \sim F(n_B-1, n_A-1)
$$

##### 何これ：前の項（11.4.4）の前提である「等分散」が本当に正しいか調べる検定
2つのグループのばらつき（不偏分散）の**「割り算（比）」**を計算します。もし2つのばらつきが全く同じなら、割り算の結果は「1」に近づくはずです。この「分散の比」が従うのが**$F$分布**であり、1からあまりにもかけ離れた数値になったら「等分散とは言えない（ばらつき方が違う）」と判定します。

---

### 11.6 適合度検定
データの分布が特定の理論に合っていないことを示すために用います。帰無仮説と対立仮説は次の通りです。

* 帰無仮説：実験結果が仮定した理論に適合する
* 対立仮説：実験結果が仮定した理論に適合しない

とりうる値が $k$ 個のカテゴリーに分割されているものとして，観測度数をそれぞれ次のようにおきます。
$$
X_1, X_2, \cdots, X_k
$$
全度数を $n$ として，各カテゴリーの期待度数をそれぞれ次のようにおきます。
$$
np_1, np_2, \cdots, np_k
$$
$n$ が十分に大きいとき，次の統計量が近似的に自由度 $k-1$ の $\chi^2$ 分布に従います。右片側検定です。
$$
\sum\limits_{i=1}^k\dfrac{(X_i-np_i)^2}{np_i} \sim \chi^2(k-1)
$$

##### 何これ：「このサイコロ、本当にすべての目が1/6の確率で出る？」を確かめる
「実際のデータ（観測度数）」と「理論上のデータ（期待度数）」のズレを計算します。
数式の分子は「（実際 - 理論）の2乗」なので、ズレが大きければ大きいほど、統計量の値は右側（大きく）へすっ飛んでいきます。だから**「右片側検定」**になります。

---

### 11.7 独立性の検定
$k$ 行 $l$ 列のクロス集計表において，2つの要因が独立ではないことを示すために用います。帰無仮説と対立仮説は次の通りです。
* 帰無仮説：行と列の要因は独立である
* 対立仮説：行と列の要因は独立でない

帰無仮説を仮定し，行と列の要因は独立であるとすると，期待度数は周辺度数の積を全度数でわって求められます。

観測度数をそれぞれ次のようにおきます。
$$
X_{ij}\quad(i\in\{1, 2, \cdots, k\}, j\in\{1, 2, \cdots, l\})
$$
また，周辺度数をそれぞれ次のようにおきます。
$$
X_{i\cdot}\quad(i\in\{1, 2, \cdots, k\}), \quad X_{\cdot j}\quad(j\in\{1, 2, \cdots, l\})
$$
全度数を $n$ として，$n$ が十分に大きいとき，次の統計量が近似的に自由度 $(k-1)(l-1)$ の $\chi^2$ 分布に従います。右片側検定です。
$$
\sum\limits_{i=1}^k\sum\limits_{j=1}^l\dfrac{\left(X_{ij}-\frac{X_{i\cdot}X_{\cdot j}}{n}\right)^2}{\frac{X_{i\cdot}X_{\cdot j}}{n}} \sim \chi^2((k-1)(l-1))
$$

##### 何これ：「性別」と「支持政党」に関係があるかを調べる
クロス集計表（アンケート結果の表など）を使って、「2つの事象が全く無関係（独立）」なのか、それとも「ドロドロに関係している」のかを暴きます。もし無関係（独立）なら、縦の合計と横の合計から綺麗に「期待度数」が計算できるはず。そこからのズレを**カイ二乗分布**でジャッジします。

---

## 12 検定（準1級範囲）
### 12.1 ポアソン分布に関する検定
$\lambda$ が大きいとき，$Po(\lambda)$ は $N(\lambda, \lambda)$ で近似できるため，$\lambda = \lambda_0$ を帰無仮説として，次の統計量が近似的に標準正規分布にしたがうと考えて検定することが可能。
$$
\dfrac{\hat{\lambda}-\lambda_0}{\sqrt{\lambda_0}} \sim N(0, 1)
$$

##### 何これ：めったに起きない事故の回数（ポアソン）も、たくさん集まれば正規分布の仲間入り
「1年間に起きる交通事故の件数」などのポアソン分布の検定です。ポアソン分布は「平均と分散がどちらも $\lambda$ になる」という面白い性質があるため、分母（標準誤差）のルートの中身が、想定している平均値である $\lambda_0$ そのものになります。

---

### 12.2 適合度検定
$(X_1, \cdots, X_k) \sim M(n, p_1, \cdots, p_k)\quad（X_1+ \cdots +X_k = n, p_1+ \cdots +p_k = 1）$ のとき，帰無仮説 $p_1 = p_{10}, \cdots, p_k = p_{k0}$ のもとでの期待度数は $np_{10}, \cdots, np_{k0}$ となります。$n$ が大きいとき，実現値を $x_1, \cdots, x_k$ として，次の検定統計量が帰無仮説のもとで近似的に自由度 $k-1$ の $\chi^2$ 分布にしたがうことを利用して検定します。
$$
\sum\limits_{i=1}^k\dfrac{(x_i-np_{i0})^2}{np_{i0}} \sim \chi^2(k-1)
$$
また，帰無仮説に $l$ 個のパラメータが含まれる場合，パラメータに最尤推定値をあてはめて期待度数を計算し，上の検定統計量が帰無仮説のもとで近似的に自由度 $k-l-1$ の $\chi^2$ 分布にしたがうことを利用します。

> [!TIP]
> **度数が極端に小さいカテゴリーがある場合**
> 検定統計量の分子を $(|x_i-np_{i0}|-0.5)^2$ とおきかえることがあり，これを**イエーツの補正**と言います。これによって，検定統計量の実現値は小さくなり，第一種の過誤を犯しにくく（第二種の過誤は犯しやすく）なります。

##### 何これ：理論値の計算に「データからの推測」が混ざると、その分だけ自由度が減る（-l）
2級の適合度検定のパワーアップ版です。例えば「データがポアソン分布に従っているか？」を確かめたい時、肝心のポアソン分布のパラメータ（$\lambda$）が分かっていないことがあります。
この時は手元のデータから最尤推定して無理やり理論値を作りますが、**「データからパラメータを1個推定するたびに、自由度が1つ（$l$個なら$l$）没収される」**というルールがあります。これが自由度 $k-l-1$ になる理由です。

---

### 12.3 尤度比検定

> [!IMPORTANT]
> **🔥 訂正：一般的な尤度比検定の定義について**
> 元のテキストでは、分子に「何でもありの最尤推定量」、分母に「帰無仮説（制約）のもとでの最尤推定量」を置いていましたが、世の中の一般的な教科書（および統計検定）では**「分子と分母が逆」**で定義されます。
> つまり、「何でもありの最大尤度（分母）」に対して、「帰無仮説という縛りプレイをした時の最大尤度（分子）」がどれくらい小さくなってしまうか、という比率 $\lambda$ を見ます。これに伴い、統計量は $-2\log\lambda$ となるため、以下のように修正・整理します。

確率（密度）関数 $f(x; \theta)$ に対して，次のような尤度比 $\lambda$ を考えます。
$$
\lambda = \dfrac{L(\hat{\boldsymbol{\theta}}')}{L(\hat{\boldsymbol{\theta}})} \quad \left( \begin{aligned} L(\hat{\boldsymbol{\theta}}') &: \text{帰無仮説（制約あり）のもとでの最大尤度} \\ L(\hat{\boldsymbol{\theta}}) &: \text{全空間（制約なし）での最大尤度} \end{aligned} \right)
$$
ただし，帰無仮説は次のようにパラメータベクトル $\boldsymbol{\theta}$ の一部分 $\boldsymbol{\theta_1}$（要素数 $p$ 個）を定数に固定するものとします。
$$
\boldsymbol{\theta} = (\theta_1, \cdots,\theta_p, \theta_{p+1}, \cdots, \theta_{p+q}) = (\boldsymbol{\theta_1}, \boldsymbol{\theta_2})
$$
このとき，帰無仮説のもとで，サンプルサイズが大きいときに近似的に，次の統計量が自由度 $p$ の $\chi^2$ 分布にしたがうことを利用して検定を行うことができます。
$$
-2\log\lambda \sim \chi^2(p)
$$

##### 何これ：仮説検定の「すべてのボス」。どんなに複雑なモデルでも比較できる万能天秤
「帰無仮説という制限をかけた状態でのもっともらしさ（分子）」と、「自由にパラメータを動かした時のもっともらしさ（分母）」の勝負です。もし帰無仮説が正しい（制限をかけても問題ない）なら、分子と分母はほぼ同じ値になり、比率 $\lambda$ は 1 に近づきます（$-2\log\lambda$ は 0 に近づく）。
逆に、制限をかけたせいで一気に尤度が下がったら、「その帰無仮説は無理がある！」と判定して棄却します。現代統計学・機械学習の検定において最も汎用性が高い強力な方法です。

---

## 13 ノンパラメトリック法
母集団分布を有限個のパラメータで記述することで推定や検定を行うのがパラメトリック法，そうでないのがノンパラメトリック法です。代表的なパラメトリック検定とノンパラメトリック検定の対応は次の表の通り。

| パラメトリック法 | ノンパラメトリック法 |
| :--- | :--- |
| 1標本 $t$ 検定 | 符号検定 |
| 対応のある2標本 $t$ 検定 | 符号付き順位検定 |
| 対応のない2標本 $t$ 検定 | 順位和検定、並べ替え検定 |
| 一元配置分散分析 | クラスカル・ウォリス検定 |

##### 何これ：データの「形」が分からなくても、順番や符号だけで戦うサバイバル統計学
これまでの検定は「データが正規分布に従っていること」が前提（パラメトリック）でしたが、現実のデータはもっといびつだったり、アンケートの「5段階評価」のように数値そのものに意味がない場合があります。
そんな時、データの具体的な数値をあきらめて、**「プラス・マイナスの符号」や「大きい順のランキング（順位）」**に変換して検定を行う、前提条件のめちゃくちゃ緩いタフな手法たちです。

---

### 13.1 符号検定
1標本の中央値の検定または対応のある2標本の差の中央値の検定。
$X_1, \cdots, X_n$ は独立に $F(x)$ を累積分布関数とする確率分布にしたがうとし，$F(x)$ は連続で，中央値がただ1つに定まるものとします。1標本で中央値が $m_0$ であるかどうかに関心がある場合には $Z = X-m_0$ とおき，2標本（$X$ と $Y$）の差の中央値に関心がある場合には $Z = X-Y$ とおきます。

> [!TIP]
> 帰無仮説：$Z$ の中央値 $= 0$，対立仮説：$Z$ の中央値 $> 0$ など
>
> * **（手順1）**
> $Z$ の符号が「＋のデータ」と「－のデータ」に分ける。0のデータは除く（除いた後のサンプルサイズを改めて $n$ とする）。
> * **（手順2）**
> ＋である $Z$ の個数 $T$ を検定統計量として，帰無仮説のもとで $T$ は $Bin(n, 1/2)$ にしたがうことを利用して $P$ 値を計算する。また，$n$ が十分に大きいときには，$Bin(n, 1/2)$ が $N(n/2, n/4)$ で近似できることを使うこともできる。

##### 何これ：大きさは無視！「勝ち」か「負け」の数だけで多数決をとる
例えば、「新商品の満足度の中央値は3（普通）より高いか？」を調べたい時、3より大きければ「＋」、小さければ「ー」とだけ記録します。もし本当に中央値が3なら、＋とーが出る確率は半々（$1/2$）のはずです。これはまさに「綺麗なコインを $n$ 回投げる二項分布 $Bin(n, 1/2)$」と同じ現象なので、そこから確率を計算します。

---

### 13.2 符号付き順位検定
1標本の中央値の検定または対応のある2標本の差の中央値の検定。符号検定の仮定に加えて，母集団分布の対称性を仮定し，帰無仮説や対立仮説は符号検定と同様です。

> [!HINT]
> * **（手順1）**
> $Z$ の符号が「＋のデータ」と「－のデータ」に分ける。0のデータは除く（除いた後のサンプルサイズを改めて $n$ とする）。
> * **（手順2）**
> $|Z|$ が小さい順に1からはじまる順位をつける。タイ（同順位）のケースについては略。
> * **（手順3）**
> 「＋のデータ」の順位の合計 $T_+$ を検定統計量とする。帰無仮説のもとでは，$1 \sim n$ の順位が $1/2$ の確率で＋か－をとるので，$2^n$ 通りのパターンの中で $T_+$ 以上に極端な結果となる確率（$P$値）を計算する。または，$n$ が十分に大きくタイ（同順位）がなければ，帰無仮説のもとで $T_+$ が近似的に次の正規分布にしたがうことを利用して $P$値を計算する。
> $$
> T_+ \sim N\left(\dfrac{n(n+1)}{4}, \dfrac{n(n+1)(2n+1)}{24} \right)
> $$

##### 何これ：符号検定に「どれくらい大きく勝ったか（順位）」の情報もプラスした贅沢版
符号検定は便利ですが、「大勝ち（＋100）」も「ギリ勝ち（＋1）」も同じ「＋」として扱うため、情報のロスが大きいです。そこで、**「ズレの絶対値が小さい順にランキング（順位）」**をつけて、「＋グループが獲得した順位の総得点」で勝負します。これにより、符号検定よりも正確に違いを見抜けるようになります。

---

### 13.3 ウィルコクソンの順位和検定
対応のない2群の差の検定。$X_1, \cdots, X_m$ は独立に $F(x)$ を累積分布関数とする確率分布にしたがい规律，$Y_1, \cdots, Y_n$ は独立に $G(x)$ を累積分布関数とする確率分布にしたがい，2標本は独立とします。$F(x)$ と $G(x)$ は分布形が同じであることを仮定します。

> [!TIP]
> 帰無仮説：$F(x) = G(x)$，対立仮説：$F(x) = G(x+\delta)$（$\delta \neq 0, \delta > 0$ など）
>
> * **（手順1）**
> 2群のデータをひとまとめにして，値が小さい順に1からはじまる順位をつける。
> * **（手順2）**
> 一方の群の順位和 $W$ を検定統計量とし，2群のサンプルサイズ $m, n$ が大きく，タイがなければ，帰無仮説のもとで検定統計量が近似的に次の正規分布にしたがうことを利用して検定を行う。
> $$
> W \sim N\left(\dfrac{m(m+n+1)}{2}, \dfrac{mn(m+n+1)}{12}\right)
> $$
> サンプルサイズが極めて小さい場合には，次の並べ替え検定と同じ方法で $P$ 値を直接求めることも可能。

##### 何これ：2つのグループを全員混ぜてランキングを作り、チーム対抗の「順位の合計点」で競う
対応のない2標本 $t$ 検定のノンパラ版です。A組とB組のデータを全部ごちゃまぜにして、全員に1位から $(m+n)$ 位までの順位をつけます。もし2つの組に実力差がないなら、A組の順位の合計（$W$）は、平均的な数値になるはず。もしA組が上位を独占して合計点数が跳ね上がったら、「2つの分布には差がある！」と結論づけます。

---

### 13.4 並べ替え検定
対応のない2群の差の検定。

> [!TIP]
> 帰無仮説：2群の母集団は共通である，対立仮説：2群の母集団は異なる
>
> * **（手順1）**
> 平均値や中央値などの統計量 $T$ を決め，標本を固定したもとで $T$ の分布をつくり，観測値以上に極端な値となる確率（$P$ 値）を求める。

##### 何これ：コンピュータの力で「あり得る全パターン」をシャッフルしてゴリ押しする検定
数式や分布の理論に頼るのを完全にやめて、「もし2つのグループに差がないなら、グループのラベル（A組・B組）をシャッフルして分け直しても、同じような平均値の差になるはずだよね」という発想です。データを実際に何万回も並べ替え、その中で「手元の本番データが見せた差」がどれくらい珍しいかをダイレクトに数え上げます。

---

### 13.5 クラスカル・ウォリス検定
3群以上の差の検定。分散分析とは異なり，母集団分布が正規分布であることを仮定することなく，データを順位におきかえて，群ごとに違いがあるかどうかを検定します。

> [!TIP]
> 帰無仮説は「すべての群の分布が等しい」，対立仮説は「少なくとも1つの群の分布が異なる」
>
> * **（手順1）**
> 独立な $k$ 群の全データをひとまとめにして，値が小さい順に1からはじまる順位をつける。
> * **（手順2）**
> 群ごとのサンプルサイズを $n_1, \cdots, n_k (n_1+ \cdots +n_k = N)$，群ごとの順位の合計を $R_1, \cdots, R_k$，群ごとの順位の平均を $\overline{R}_1, \cdots, \overline{R}_k$ として，タイがなければ検定統計量は，
> $$
> H = \dfrac{12}{N(N+1)}\sum\limits_{i=1}^kn_i\left(\overline{R}_i-\dfrac{N+1}{2}\right)^2
> $$
> この検定統計量が，サンプルサイズが大きいときに帰無仮説のもとで近似的に自由度 $k-1$ の $\chi^2$ 分布にしたがうことを利用して検定を行います。
> $$
> H \sim \chi^2(k-1)
> $$

##### 何これ：一元配置分散分析（ANOVA）のノンパラ版。3チーム以上の順位戦
ウィルコクソンの順位和検定（13.3）を「3グループ以上」に拡張したものです。全員分のランキングを作った後、各グループの「平均順位（$\overline{R}_i$）」が、全体の真ん中の順位（$\frac{N+1}{2}$）からどれくらい離れているかを計算し、そのズレを**カイ二乗分布**で評価します。

---

### 13.6 順位相関係数
2変数の順序尺度データの相関係数，または，2変数の量的データを順位におきかえて計算した相関係数。外れ値の影響を受けにくいという特徴があります。サイズ $n$ の2変量順位データを $(x_1, y_1), \cdots, (x_n, y_n)$ とすると，タイがなければ，$1 \sim n$ の平均が $\dfrac{n+1}{2}$ であることから，2変数の相関係数は次のように計算できます。
$$
r_s = 1-\dfrac{6}{n(n^2-1)}\sum\limits_{i=1}^n(x_i-y_i)^2
$$
上の $r_s$ を**スピアマンの順位相関係数**と言います。

一方で，2組の順位データ $(x_i, y_i)$ と $(x_j, y_j) （i \neq j）$ について，$(x_i-x_j)(y_i-y_j)$ が正ならば ＋1，$(x_i-x_j)(y_i-y_j)$ が負ならば －1 として，$n(n-1)/2$ 組の平均を計算したものが**ケンドールの順位相関係数**であり，$(x_i-x_j)(y_i-y_j)$ が正になる組の数を $P$，負になる組の数を $N$ として，次の式で表せます。
$$
r_k = \dfrac{2(P-N)}{n(n-1)}
$$

##### 何これ：とんでもない大富豪（外れ値）が混ざっていても騙されない、タフな相関関係
普通の相関係数（ピアソン）は、データの中に1個でも桁違いにデカい値（外れ値）があると結果がグチャグチャになります。しかし、データを「1位、2位、3位…」という順位に変えてしまえば、100のデータも1億のデータもただの「1位」になり、外れ値の影響を完全に無効化できます。
* **スピアマン：** 「順位そのもの」を使って普通の相関を計算する。
* **ケンドール：** 「A君がB君より数学の順位が上なら、英語の順位も上か？」という「2人の間の上下関係のハラ落ち度（一致ペア・不一致ペア）」を数え上げる。

---

## 14 マルコフ連鎖
$\{1, 2, \cdots, N\}$ のいずれかの値をとる確率変数 $X_n$ を，時点 $n = 0, 1, 2, \cdots$ の順に並べた確率変数列 $X_0, X_1, X_2, \cdots$ が，次の式を満たすとき，この確率変数列を**マルコフ連鎖**と言います。
$$
P(X_{n+1}=j \mid X_n=i, X_{n-1}=i_{n-1}, \cdots, X_0=i_0) = P(X_{n+1}=j \mid X_n=i)
$$
とりうる状態の全体 $\{1, 2, \cdots, N\}$ を**状態空間**と言い，$i$ や $j$ は $\{1, 2, \cdots, N\}$ のいずれかを表します。このような条件付き確率が $n$ によらずに $i$ と $j$ だけできまる場合を斉時的と言い，その場合だけを考えれば十分です。

##### 何これ：「過去の履歴」は一切関係ない。明日の天気が決まるのは「今日の天気」だけという世界
「未来の状態がどうなるかは、現在の状態だけで決まり、それより前の過去の歴史は一切関係ない」という究極の割り切り（**マルコフ性**）を持った確率モデルです。
例えば、「今日が雨（$X_n=i$）なら、明日が晴れる確率（$X_{n+1}=j$）は30%」のように、前日までの天気の流れがどうだったかは完全に無視して、今日の状態だけで次のステップへの移動確率が確定します。

---

### 14.1 状態確率ベクトル
時点 $n$ でそれぞれの状態にある確率を並べたベクトル $\Pi_n = (p_n(1), \cdots, p_n(N))$ のこと。特に，$\Pi_0$ を**初期分布**と言います。

##### 何これ：現時点で「どの状態に何%の確率で滞在しているか」の割り当て表
例えば、「[晴れ, 曇り, 雨] = [60%, 30%, 10%]」のように、現在の状態の確率を横一列に並べたものです。スタート時点の確率を「初期分布」と呼びます。

---

### 14.2 推移確率行列
$\Pi_{n+1} = \Pi_nQ$ を満たし，時点 $n$ の各状態から時点 $n+1$ の各状態へ移る確率をまとめた $N$次正方行列 $Q$。

##### 何これ：状態を変化させる「サイコロのルール一覧表」
状態Aから状態Bへ、状態Bから状態Cへ…という、あらゆる移動確率を網羅したマトリクスです。状態確率ベクトルにこの行列 $Q$ を右からパチんと掛け算するだけで、「1ステップ先の未来の確率ベクトル」を瞬時に計算できます。

---

### 14.3 定常分布
$\Pi = \Pi Q$ を満たす状態確率ベクトル $\Pi$ のこと。ある条件のもとで，マルコフ連鎖はただ1つの定常分布をもちます。

##### 何これ：何回推移を繰り返しても、もう確率の割合が変化しなくなる「究極の黄金バランス」
推移確率行列 $Q$ を何回も何回も掛け続けていくと、ある時点で「[晴れ, 曇り, 雨] = [50%, 30%, 20%]」のまま、次の日も、その次の日も全く数字が動かなくなる状態（均衡状態）にたどり着くことがあります。
この「歴史の果てに行き着く安定した分布」のことを定常分布と呼びます。Googleの初期の検索エンジンアルゴリズム（ページランク）も、この定常分布の仕組みを使ってWebページの重要度を計算していました。

---

### 14.4 パラメータの推定
推移確率行列がパラメータ $\theta$ を含むとき，観測データ $X_0 = x_0, X_1 = x_1, \cdots, X_n = x_n$ を用いて $\theta$ を最尤推定するには，次の変形を利用します。
$$
\begin{aligned}
    P(X_0=x_0, X_1=x_1, \cdots, X_n=x_n) &= P(X_n=x_n \mid X_{n-1}= x_{n-1})P(X_0=x_0, \cdots, X_{n-1}=x_{n-1}) \\
    &= \cdots \\
    &= P(X_n=x_n \mid X_{n-1}=x_{n-1})P(X_{n-1}=x_{n-1} \mid X_{n-2}=x_{n-2}) \cdots P(X_1=x_1 \mid X_0=x_0)P(X_0=x_0)
\end{aligned}
$$
これらの積の要素一つ一つが遷移確率行列の成分を意味する。

初期分布 $P(X_0 = x_0)$ が定数として weddings 与えられれば，同時確率 $P(X_0 = x_0, X_1 = x_1, \cdots, X_n = x_n)$ は推移確率行列の成分の積で表せ，$\theta$ の関数になります。これを尤度関数として，最尤推定値を求めます。

##### 何これ：過去の足跡（時系列データ）をバラバラに分解して、一番しっくりくるルール（θ）を逆算する
「晴れ→晴れ→雨→曇り→晴れ」という実際の天気のデータが手元にあるとき、全体の起きる確率は、「（晴れ→晴れ）の確率 × （晴れ→雨）の確率 × （雨→曇り）の確率…」というように、**1ステップずつの移動確率の掛け算**へと綺麗にバラバラに分解できます。
マルコフ性のおかげで、複雑な時系列データもただの「前日と当日のペアの掛け算」になるため、最尤法（もっともらしさの計算）がめちゃくちゃ簡単になるという大きなメリットがあります。

---

## 15 確率過程の基礎

### 15.1 確率過程
$X_1, X_2, \cdots$ のような確率変数列 $X = (X_t)$ を**離散時間の確率過程**，$t$ が0以上のすべての実数をとるとき，$X = (X_t)$ を**連続時間の確率過程**と言います。

##### 何これ：時間とともに変化していく「確率の連続ドラマ」
これまでの統計学は「ある1時点のデータ」を切り取って分析していましたが、確率過程は「時間の流れ」を組み込みます。
* **離散時間：** 「毎日の終値」のように、1日目、2日目…とコマ送りでパラパラ記録する株価データなど。
* **連続時間：** 「現在の気温」のように、1秒の隙間もなくヌルヌルと途切れることなく変化し続けるデータなど。

---

### 15.2 ブラウン運動
離散時間の確率過程であるランダムウォークの時間間隔を短くした極限として得られる連続時間の確率過程で，次の条件を満たす $B = (B_t)$ のこと。
1. $B_0 = 0$ で $B$ のパスは連続
2. 【独立増分性】任意の時間分割 $0 = t_0 < t_1 < \cdots < t_n$ に対して，次のような $n$ 個の増分は互いに独立
$$
B_{t_1}-B_{t_0}, B_{t_2}-B_{t_1}, \cdots, B_{t_n}-B_{t_{n-1}}
$$
3. 【定常増分性】任意の $t > s \ge 0$ に対して，増分は，次のような正規分布にしたがう
$$
B_t-B_s \sim N(\mu(t-s), \sigma^2(t-s))
$$

ブラウン運動のパラメータ $\mu, \sigma$ は，一定の時間間隔 $\Delta$ で観測したときの増分 $Z_k$ が $N(\mu\Delta, \sigma^2\Delta)$ にしたがうことを利用して，次の式によって推定します。
$$
\begin{cases}
    \hat{\mu}\Delta = \dfrac{1}{n}\sum\limits_{k=1}^nZ_k \\
    \hat{\sigma}^2\Delta = \dfrac{1}{n}\sum\limits_{k=1}^n(Z_k-\hat{\mu}\Delta)^2
\end{cases}
$$

##### 何これ：過去を一切引きずらず、常にサイコロを振ってフラフラと彷徨う究極のランダム
水に浮かべた花粉の微粒子が、水分子にぶつかってデタラメに動く様子から名付けられたモデルで、**株価や為替の動き（ブラック・ショールズ・モデルなど）の基礎**になります。
* **独立増分性：** 「昨日上がったから今日は下がるだろう」というような過去の記憶が一切通用せず、毎瞬間の動きが完全に独立しているということ。
* **定常増分性：** どんな時間帯を切り取っても、その変動幅（増分）は「経過時間（$t-s$）に比例してばらつきが大きくなる正規分布」に必ず従うということ。

---

### 15.3 ポアソン過程
店に訪れる客の数のように，0以上の整数値をとる連続時間の確率過程で，次の条件を満たす $N = (N_t)$ が強度 $\lambda$ のポアソン過程です。
1. $N_0 = 0$
2. 【独立増分性】任意の時間分割 $0 = t_0 < t_1 < \cdots < t_n$ に対して，次のような $n$ 個の増分は互いに独立
$$
N_{t_1}-N_{t_0}, N_{t_2}-N_{t_1}, \cdots, N_{t_n}-N_{t_{n-1}}
$$
3. 【定常増分性】任意の $t > 0, s \ge 0$ に対して，
$$
P(N_{s+t}-N_s=k) = \dfrac{e^{-\lambda t}(\lambda t)^k}{k!} \quad (\text{for}~k=0, 1, 2, \cdots)
$$

強度 $\lambda$ のポアソン過程で，イベントの発生時刻を $T_1, T_2, \cdots, T_n$ として，イベント発生の時間間隔 $W_k = T_k - T_{k-1} \quad (k = 1, 2, \cdots, n)$ はパラメータ $\lambda$ の指数分布に独立にしたがうことから，$\lambda$ は $n/T_n$ によって推定できることがわかります。

また別の方法として，時間間隔 $\Delta$ ごとに $n$ 回の観測を行い，観測されたイベントの回数を $N_\Delta, N_{2\Delta}, \cdots, N_{n\Delta}$ とすると，$\Delta$ の間に発生したイベントの数 $M_k = N_{k\Delta} - N_{(k-1)\Delta} \quad (k = 1, 2, \cdots, n)$ は平均 $\lambda\Delta$ のポアソン分布に独立にしたがうことから，$\lambda$ は $N_{n\Delta}/n\Delta$ によって推定できることがわかります。
どちらの場合も，（イベントの総数）$\div$（観測期間）が推定値になります。

##### 何これ：「いつ起きるか分からない出来事」を、ひたすらカチカチと数え続けるカウンター
「コンビニにお客さんが来る」「交通事故が起きる」など、突発的なイベントが時間とともにどれくらい発生するかをカウントしていくモデルです。
* **回数はポアソン、間隔は指数：** ここが超重要です。ある時間内に「何回イベントが起きたか」はポアソン分布に従いますが、イベントとイベントの「待ち時間（間隔）」は指数分布に従います。これは統計検定でも鉄板のセットです。

---

### 15.4 複合ポアソン過程
あとの1., 2.の条件のもとで，次の式で定まる確率過程 $X = (X_t)$
$$
X_t = \sum\limits_{k=1}^{N_t}U_k
$$
1. $N = (N_t)$ はポアソン過程
2. $(U_k)$ は独立に同じ分布にしたがう確率変数列で，$N$ とも独立

複合ポアソン過程 $X = (X_t)$ について，$E[N_t] = \lambda t, E[U_k] = \mu, V[U_k] = \sigma^2$ のとき，$E[X_t] = \lambda\mu t, V[X_t] = \lambda t(\mu^2 + \sigma^2)$ が成り立ちます。

##### 何これ：「お客さんが来るか？」に加えて、「いくらお金を落としていくか？」も掛け合わせた現実モデル
ポアソン過程は「回数（人数）」しか数えられませんでしたが、複合ポアソン過程は**「イベント1回ごとのインパクトの大きさ（$U_k$）」**もランダムであると仮定して合計値を出します。
* **ビジネスの例：** お客さんが来る人数がポアソン過程（$N_t$）。そのお客さんが買っていく金額もランダム（$U_k$）。1日の総売上が複合ポアソン過程（$X_t$）。
* **保険の例：** 交通事故が起きる件数がポアソン過程（$N_t$）。その事故で支払う保険金の額もランダム（$U_k$）。保険会社が支払う総額が複合ポアソン過程（$X_t$）。
全体のばらつき（分散）の式に、回数のばらつきと金額のばらつきが合体しているのが特徴です。


## 16 重回帰分析
観測値ベクトル $\boldsymbol{y} = (y_1, \cdots, y_n)^T$，説明変数行列 $X = (x_{ij})$，回帰係数ベクトル $\boldsymbol{\beta} = (\beta_0, \cdots, \beta_d)^T$，誤差ベクトル $\boldsymbol{\epsilon} = (\epsilon_1, \cdots, \epsilon_n)^T$ に対し，$\boldsymbol{y} = X\boldsymbol{\beta} + \boldsymbol{\epsilon}$ は，次のように $n$ 本の式をまとめたものです。

$$
\begin{aligned}
    \begin{pmatrix}
    y_1 \\
    y_2 \\
    \vdots \\
    y_n
    \end{pmatrix}
    =
    \begin{pmatrix}
    1 & x_{11} & \cdots & x_{1d} \\
    1 & x_{21} & \cdots & x_{2d} \\
    \vdots & \vdots & \ddots & \vdots \\
    1 & x_{n1} & \cdots & x_{nd}
    \end{pmatrix}
    \begin{pmatrix}
    \beta_0 \\
    \beta_1 \\
    \vdots \\
    \beta_d
    \end{pmatrix}
    +
    \begin{pmatrix}
    \varepsilon_1 \\
    \varepsilon_2 \\
    \vdots \\
    \varepsilon_n
    \end{pmatrix}
\end{aligned}
$$

##### 何これ：「いくつかの原因（$x$）」から、「1つの結果（$y$）」を予想する最強の方程式
例えば、「部屋の広さ」「駅からの徒歩分数」「築年数」という3つの原因（説明変数）から、「家賃」という結果（目的変数）をピタリと当てる数式（モデル）を作ります。行列で書くと難解に見えますが、中身はただの連立方程式の大量まとめセットです。

---

### 16.1 最小2乗法
$X$ を定数行列とみなし，$X^TX$ が正則（逆行列を持つ）であるならば，誤差平方和 $\boldsymbol{\epsilon}^T\boldsymbol{\epsilon}$ が最小になるような $\boldsymbol{\beta}$ は次の式のように求められます。
$$
\boldsymbol{\hat{\beta}} = (X^TX)^{-1}X^T\boldsymbol{y}
$$
これを**最小2乗推定量**と言います。$E[\boldsymbol{\epsilon}] = \boldsymbol{0}$ であるとき，最小2乗推定量は $\boldsymbol{\beta}$ の不偏推定量です。

> [!IMPORTANT]
> **🔥 訂正：分散共分散行列の数式表記について**
> 元のテキストでは `$V[\boldsymbol{\epsilon}] = E[\boldsymbol{\epsilon}^T\boldsymbol{\epsilon}] = \sigma^2\boldsymbol{I}$` となっていましたが、$\boldsymbol{\epsilon}$ は $n \times 1$ の縦ベクトルのため、`$\boldsymbol{\epsilon}^T\boldsymbol{\epsilon}$` だとスカラー（1つの数値）になってしまい行列になりません。
> 正しくは、縦×横の順に掛け合わせた外積 `$\boldsymbol{\epsilon}\boldsymbol{\epsilon}^T$`（$n \times n$ 行列）になります。以下のように正しく修正します。

誤差ベクトル $\boldsymbol{\epsilon}$ の分散共分散行列が $V[\boldsymbol{\epsilon}] = E[\boldsymbol{\epsilon}\boldsymbol{\epsilon}^T] = \sigma^2\boldsymbol{I}$ と表されるとき，最小2乗推定量の分散共分散行列は $\sigma^2(X^TX)^{-1}$ となります。最小2乗推定量は $\boldsymbol{\beta}$ の最良線形不偏推定量（BLUE）であり，これを**ガウス・マルコフの定理**と言います。

最小2乗推定量を用いてつくった回帰式から予測される値を並べて，次のように**予測値ベクトル**を定めます。
$$
\boldsymbol{\hat{y}} = X\boldsymbol{\hat{\beta}} = X(X^TX)^{-1}X^T\boldsymbol{y}
$$
また，**残差ベクトル** $\boldsymbol{e}$ は，観測値ベクトルと予測値ベクトルの差として，次のように定めます。
$$
\boldsymbol{e} = \boldsymbol{y} - \boldsymbol{\hat{y}} = \boldsymbol{y} - X\boldsymbol{\hat{\beta}}
$$
残差の自由度は $n - d - 1$ であり，残差平方和 $\boldsymbol{e}^T\boldsymbol{e}$ をその自由度でわったものが誤差分散 $\sigma^2$ の不偏推定量になります。

##### 何これ：データと予測線の「ズレ」が一番小さくなるように、直線をピタッと合わせる職人技
グラフの上に散らばったデータたちに対して、最も綺麗にフィットする直線を引く方法です。それぞれのデータから直線までの「タテのズレ（残差）」を2乗して全部足し算し、それが「一番小さくなる場所」を微分で一発計算します。ガウス・マルコフの定理は、「いくつかの前提さえ満たせば、この方法で出した数式が世の中で一番エコで正確（最良）だよ」とお墨付きを与えてくれています。

---

### 16.2 決定係数
次の式のような総変動に対する回帰変動の割合のこと。0以上1以下の値をとり，1に近いほど回帰式のデータへのあてはまりが良いと解釈できますが，モデルの予測性能を表すものではありません。また，説明変数を増やすだけで値が大きくなってしまうという欠点があります。
$$
\begin{aligned}
    R^2 &= \dfrac{\sum\limits_{i=1}^n \left( \hat{\beta}_1(x_{i1}-\overline{x}_1) + \cdots + \hat{\beta}_d(x_{id}-\overline{x}_d) \right)^2}{\sum\limits_{i=1}^n (y_i-\overline{y})^2} \\
    &= 1 - \dfrac{\sum\limits_{i=1}^n e_i^2}{\sum\limits_{i=1}^n (y_i-\overline{y})^2}
\end{aligned}
$$

##### 何これ：「この予測方程式は、現実を何%くらい説明できているか？」の成績表
「この家賃予測モデルの正解率は 85%（$R^2 = 0.85$）です！」と言えるための指標です。
ただし、これには重大な弱点があります。例えば、家賃とは全く関係のない「大家さんの今日の気分」や「お隣さんのペットの名前」のような**ゴミデータ（無関係な説明変数）をモデルに追加していくだけで、この数値は勝手にどんどん上がってしまいます。** そのため、これ単体で調子に乗ってモデルを複雑にするのは危険です。

---

### 16.3 自由度調整済み決定係数
次の式のように，決定係数に自由度による調整を加えたもので，説明変数を増やしただけでは必ずしも値が大きくはなりません。
$$
\begin{aligned}
R^{*2} &= 1 - \dfrac{\frac{1}{n-d-1} \sum\limits_{i=1}^n e_i^2}{\frac{1}{n-1} \sum\limits_{i=1}^n (y_i - \bar{y})^2} \\
&= 1 - \dfrac{n-1}{n-d-1} (1 - R^2)
\end{aligned}
$$

##### 何これ：無駄なおまけ（変数）を増やすとペナルティを食らう、厳しめの実力判定シート
16.2 の「変数増やせば数値が上がっちゃうバグ」を修正した、大人の決定係数です。
説明変数（$d$）を1つ増やすたびに、分母の自由度（$n-d-1$）が減って、数式全体にペナルティが課される仕組みになっています。本当に意味のある強力な変数（原因）を入れない限り、数値が逆に下がってしまうため、正しいモデル選びに必須の指標です。

---

### 16.4 回帰係数の推定と検定
誤差ベクトル $\boldsymbol{\epsilon}$ が $n$ 変量正規分布 $N(\boldsymbol{0}, \sigma^2\boldsymbol{I})$ にしたがうことを仮定すると，最小2乗推定量は $(d+1)$ 変量正規分布 $N(\boldsymbol{\beta}, \sigma^2(X^TX)^{-1})$ にしたがうことを利用して，回帰係数の推定や検定を行うことができます。

例えば，$\beta_i = 0$ を帰無仮説とするとき，（$\beta_i$ の推定量）/（$\beta_i$ の標準誤差）は自由度 $n-d-1$ の $t$ 分布にしたがい，$\beta_1 = \beta_2 = \cdots = \beta_d = 0$ を帰無仮説とするとき，次の統計量が自由度 $(d, n-d-1)$ の $F$ 分布にしたがうことを利用します。
$$
\dfrac{\dfrac{1}{d} \sum\limits_{i=1}^n \left\{ \hat{\beta}_1 (x_{i1} - \bar{x}_1) + \dots + \hat{\beta}_d (x_{id} - \bar{x}_d) \right\}^2}{\dfrac{1}{n-d-1} \sum\limits_{i=1}^n e_i^2} \sim F(d, n-d-1)
$$

##### 何これ：その原因（変数）は、本当に結果に影響を与えているか裁判にかける
* **$t$検定（個別チェック）：** 「『築年数』の係数（$\beta_i$）は、たまたま数字が出ただけ（本当は0）じゃないか？」を個別に調べます。
* **$F$検定（全員一括チェック）：** 「このモデルに入れた説明変数たち、全員揃いも揃って全く役に立っていない（全員 $\beta = 0$）んじゃないか？」をまとめて一発でジャッジします。

---

### 16.5 一般化最小2乗法
回帰モデルの誤差項が不均一分散（データの場所によってブレの大きさが違う）であったり，相関がある（前後のデータがベタベタ影響し合っている）場合には，通常の最小2乗推定量はBLUE（最良）ではなくなり，適切な変換を組み込んだ一般化最小2乗推定量がBLUEになります。

##### 何これ：データのばらつきが不平等な時に、エコひいきを無くすために平らにならす技
普通の最小2乗法は「どのデータも、ばらつきのガチャ度合いは平等（等分散）」という前提で戦っています。しかし、例えば「収入が上がるほど、食費の個人差（ばらつき）がめちゃくちゃデカくなる」というように、データのエリアによってブレ幅が違うことがあります。この不平等を、数式トリックで「均一」に変形してから解くのが一般化最小2乗法です。

---

### 16.6 正則化
例えば，多項式回帰モデル $y = \beta_0 + \beta_1x + \cdots + \beta_dx^d$ の次数を十分に大きくすると，誤差2乗和を0にすることができますが，非常に大きな絶対値の回帰係数を用いて学習データに無理やり合わせたモデルとなってしまいます（**過学習**）。これでは予測性能の面で使いものにならないので，正則化項を追加することで回帰係数の絶対値を制御し，モデルが複雑になりすぎないようにします。

##### 何これ：過去問（手元のデータ）を丸暗記しすぎて、本番の試験（未来の予測）で大爆死するのを防ぐ
説明変数をめちゃくちゃ多くしたり、複雑なカーブを描く数式にすると、手元にあるデータの点（過去問）を「100%完璧にすべて通る線」が作れてしまいます。しかし、それはデータの「ノイズ」まで丸暗記した歪んだ線です。
新しいデータが来た時に全く使えなくなる（過学習）のを防ぐために、数式に**「係数をあんまり大きくするなよ！」という縛り（ペナルティ）**をわざと課して、線を滑らかに保ちます。

---

### 16.7 L2正則化（リッジ回帰）
定数項を除くパラメータの2乗和を正則化項として，次の式全体が最小になるようにパラメータを推定します。
$$
\sum_{i=1}^n \left[ y_i - \left( \beta_0 + \sum_{j=1}^d \beta_j x_{ij} \right) \right]^2 + \lambda \sum_{j=1}^d \beta_j^2
$$
$\lambda$ は正則化パラメータと呼ばれ，$\lambda = 0$ ならば通常の最小2乗法と一致します。$\lambda$ を大きくすると，正則化項の影響が大きくなり，回帰係数は0に近い値をとるようになります。

##### 何これ：みんなで少しずつ遠慮し合って、極端に尖ったボスを作らない平和主義モデル
右側のペナルティ項（$\lambda \sum \beta^2$）のせいで、誰か1人の係数（$\beta$）がめちゃくちゃデカい値になると、ペナルティが爆発してしまいます。
そのため、すべての変数の係数が**「みんなで少しずつ小さくなって、お互い控えめに影響を分け合おう」**という動きになります。変数を完全に消去（0に）はしませんが、全体をマイルドに縮小させる（収縮させる）のが特徴です。

---

### 16.8 L1正則化（Lasso回帰）
定数項を除くパラメータの絶対値の和を正則化項として，次の式全体が最小になるようにパラメータを推定します。
$$
\sum_{i=1}^n \left[ y_i - \left( \beta_0 + \sum_{j=1}^d \beta_j x_{ij} \right) \right]^2 + \lambda \sum_{j=1}^d |\beta_j|
$$
回帰係数の値の多くを，0に近い値ではなく，0と推定する傾向（**スパース性**）があるため，パラメータの推定とモデル選択を同時に行うことができます。なお，正則化パラメータ $\lambda$ の適切な値を決める方法としては，第30章で説明する交差検証法があります。

##### 何これ：役に立たない変数を容赦なく「クビ（完全にゼロ）」にする、断捨離の鬼モデル
リッジ回帰と違い、こちらは絶対値（$|\beta|$）で縛りを入れます。この数学的な性質のおかげで、**「ちょっとでも役に立たないと判断された変数の $\beta$ は、容赦なくジャスト0になる」**という強烈な特徴（スパース性）が生まれます。
100個ある変数のうち、重要な5個だけを残して残りの95個を自動的に削ってくれるため、「モデルのすっきり化（変数選択）」が同時に達成できます。

---

### 16.9 正則化パス
横軸に $\ln\lambda$，縦軸に回帰係数の値をとり，正則化パラメータの値の変化に対して，それぞれの回帰係数の値の変化をグラフにしたものです。

* **リッジ回帰における正則化パスの例：** $\ln\lambda$ が大きくなると，回帰係数がみんな揃ってジワジワと0に向かって縮んでいくことが見てとれます。
  ![Ridge回帰の係数軌跡](https://toketarou.com/wp-content/uploads/2023/12/e99b81597f220e8c44d42ab93012df93-1024x667.png)

* **Lasso回帰における正則化パスの例：** $\ln\lambda$ が大きくなる（縛りがキツくなる）につれて，回帰係数がガクンと真に0（水平なゼロの線）になって脱落していくものが増えていくことが見てとれます。
  ![alt](https://toketarou.com/wp-content/uploads/2023/12/582272ba46591745e784911ff248f04f-1024x655.png)

##### 何これ：縛りをキツくしていった時に、誰が最後まで生き残るかの「サバイバル軌跡チャート」
「ペナルティの厳しさ（$\lambda$）」をどんどん上げていったときに、それぞれの変数の影響力（係数）がどう変化するかを記録した人生ゲームのグラフです。
Lassoのグラフを見ると、あるポイントでパサッと0の床に叩きつけられて消滅する（変数選択される）様子が視覚的にめちゃくちゃよく分かります。

---

### 16.10 Elastic Net
L1正則化とL2正則化を混ぜたもので，変数選択と係数の縮小を同時に行います。変数間の相関が高い場合などに有効です。

##### 何これ：リッジとラッソのいいとこ取り。仲良しグループを一緒に採用するハイブリッド
Lasso（L1）は非常に便利なのですが、「似たような意味を持つ、相関のめちゃくちゃ高い変数グループ（例：『部屋の広さ』と『畳の枚数』）」がいた場合、気まぐれにどちらか片方だけを残して、もう片方を強引に0にしてしまうという気性の荒さがあります。
ここにリッジ（L2）の優しさを混ぜることで、**「仲良しグループはみんな一緒に係数を小さくしつつ（L2）、完全に不要なゴミは0にする（L1）」**という器用な立ち回りが可能になります。

---

### 16.11 Fused lasso
隣接する回帰係数の差に罰則を課すL1正則化で，隣接する回帰係数が同じ値になりやすくなります。時系列データなどに適用すると，時間的に滑らかな変化を示すモデルを構築できます。

##### 何これ：「お隣さん同士は、きっと同じような値のはず」という隣人縛り
「1月、2月、3月…」のような時系列データや、ゲノムの配列データを扱う時の特殊技です。
「先月の影響力と、今月の影響力が、いきなり真逆になるわけないよね？」という前提のもと、**「隣り合う係数同士の引き算（差分）」にもL1ペナルティ**をかけます。その結果、お隣さん同士が綺麗に同じ値に張り付きやすくなり、グラフの変化が階段状、あるいは滑らかなステップ状になって非常に見やすくなります。