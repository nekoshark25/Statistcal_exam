## 19 回帰分析その他

### 19.1 トービットモデル

$Y$を目的変数とした回帰モデルをつくりたいが，$Y$が0以上の値しかとらないというケースがよくあります。このような場合に，$Y^*＝\beta_0＋\beta_1X_1＋\beta_2X_2＋\epsilon$などの回帰モデルで表せる潜在変数$Y^*$を考え，$Y^*$が0以下の場合には$Y$＝0，$Y^*$が0より大きい場合には$Y＝Y^*$とするのがトービットモデルの例です。
では，データ$(x_1，y_1)\cdots(x_n，y_n)$に対して，次のトービットモデルを考え，尤度関数を構成します。
$$
\begin{aligned}
    \begin{cases}
        y_i^*=\beta_0+\beta_1x_i+\epsilon_i\qquad(i=1,2,\cdots, n)\\
        y_i=\begin{cases}
            y_i^*\qquad(\text{for}~~y_i^*>0)\\
            0~~\qquad\text{(else)}
        \end{cases}
    \end{cases}
\end{aligned}
$$
ここで，$\epsilon_i$は互いに独立に正規分布$N(0，\sigma^2)$にしたがう誤差項であるとすると，$x_i$を与えたもとで，$y_i^*$は次の正規分布にしたがいます。
$$
\begin{aligned}
    y_i^*\sim N(\beta_0+\beta_1x_i, \sigma^2)
\end{aligned}
$$
つまり，$y_i^*>0$のとき，$y_i（=y_i^*）$の確率密度関数は次の式になります。
$$
\dfrac{1}{\sqrt{2\pi\sigma^2}}\exp\left(-\dfrac{1}{2}\left(\dfrac{y_i-(\beta_0+\beta_1x_i)}{\sigma} \right)^2 \right)
$$
標準正規分布の確率密度関数を$\phi(x)$とすると，上の$y_i$の確率密度関数は次のように表せます。
$$
\begin{aligned}
    \dfrac{1}{\sigma}\phi\left(\dfrac{y_i-(\beta_0+\beta_1x_i)}{\sigma} \right)
\end{aligned}
$$
次に，$y_i^*\leq0$となる確率は，$y_i^*$の確率密度関数を使って次のように表せます。
$$
P(y_i^* \le 0) = \int_{-\infty}^{0} \frac{1}{\sigma} \phi \left( \frac{y - (\beta_0 + \beta_1 x_i)}{\sigma} \right) dy
$$
上の被積分関数$\phi(\cdot)$のかっこ内をzと置換すると，次のようになります。
$$
\int_{-\infty}^{-\frac{\beta_0 + \beta_1 x_i}{\sigma}} \phi(z)dz = \Phi \left( -\frac{\beta_0 + \beta_1 x_i}{\sigma} \right)
$$
ここで，上の式の右辺の$\Phi(\cdot)$は，標準正規分布の累積分布関数です。よって，尤度関数は，$y_i^*>0$となるiに関する確率密度関数の積と$y_i^*\leq0$となるiに関する累積分布関数の積を合わせて，次のようになります。
$$
\prod_{i:y_i > 0} \frac{1}{\sigma} \phi \left( \frac{y_i - (\beta_0 + \beta_1 x_i)}{\sigma} \right) \times \prod_{i:y_i = 0} \Phi \left( -\frac{\beta_0 + \beta_1 x_i}{\sigma} \right)
$$

*   **何これ：** 下限（または上限）でデータが「壁」にぶつかって止まっている状態を扱うモデル
>[!tip] **たとえ話：0点未満がないテスト**
>本当は「実力がマイナス」の人もいるはずなのに、テストの点数は最低でも0点にしかなりません。このとき、0点の集団には「本当は-10点の人」や「本当は-50点の人」が混ざっています。トービットモデルは、この「壁（0点）」に張り付いた人たちの裏側にある「潜在的な実力 $y^*$ 」を、正規分布を仮定して推定する賢い仕組みです。

### 19.2 生存時間
入院してから退院するまでの時間や就職してから退職するまでの時間など，基準となる時点からイベントが発生するまでの時間。

### 19.3 生存関数
生存時間が$t$以上である確率のことであり，生存時間を表す確率変数$T$の確率密度関数を$f(x)$として，次の式で表されるような$S(t)$です。
$$S(t) = P(T \geq t) = \int_t^\infty f(x)dx$$
累積分布関数を$F(t)$として，$S(t)＝１-F(t)$の関係があり，$S(t)$を$t$で微分すると，次のようになります。
$$
\frac{d}{dt}S(t) = \frac{d}{dt}\{1 - F(t)\} = -f(t)
$$

### 19.4 ハザード関数

時刻tにおける瞬間イベント発生率（時刻tまでイベントが発生していない条件のもとで直後にイベントが発生する条件付き確率）を表す関数で，次のように確率密度関数と生存関数を用いて表せる$h(t)$です。

$$
h(t) = \frac{f(t)}{S(t)} = -\frac{d}{dt} \log S(t)
$$


### 19.5 累積ハザード関数
次のようにハザード関数を積分したものであり，最右辺のように生存関数を用いて表せます。

$$
H(t) = \int_{0}^{t} h(x)dx = -\log S(t)
$$
確率密度関数は，ハザード関数と累積ハザード関数を用いて次のように表せます。
$$
f(t) = h(t) \exp[-H(t)]
$$

*   **何これ：** 「いつ終わるか？」という寿命や待ち時間を分析する「生存時間解析」の三種の神器
>[!tip] **たとえ話：生存・脱落のドラマ**
>*   **生存関数 $S(t)$ ：** 時刻 $t$ までに「生き残っている（イベントが起きていない）」確率。時間が経つほど減っていきます。
>*   **ハザード関数 $h(t)$ ：** 「瞬間的なリスク」。今この瞬間まで無事だったものが、次の瞬間にドカンとイベント（故障や死亡）に見舞われる確率の「濃さ」です。
>*   **累積ハザード関数 $H(t)$ ：** これまでのリスクの「積み重ね」。これが生存関数の対数（マイナス付き）に対応しているのが数理的な面白さです。

### 19.6 カプラン・マイヤー法
生存時間に特定の確率分布を仮定することなく，生存関数を推定する方法。例えば，時点$t$までに，次の表のような$k$時点でイベントが発生した場合を考えます。
| 項目 | 値 |
| :--- | :--- |
| **イベントの発生時点** | $t_1, t_2, \cdots, t_k$ |
| **イベントの発生数** | $d_1, d_2, \cdots, d_k$ |
| **直前のリスク集合** | $n_1, n_2, \cdots, n_k$ |

このとき，時点$t$における生存率を次の式で計算します。
$$
\frac{n_1 - d_1}{n_1} \times \frac{n_2 - d_2}{n_2} \times \dots \times \frac{n_k - d_k}{n_k}
$$
このような計算を観察期間全体について行い，その結果をプロットしてできる階段状のグラフを**カプラン・マイヤー曲線**と言います。

*   **何これ：** データの「脱落者」を数えるだけで描ける、もっとも有名な生存曲線
>[!tip] **たとえ話：サバイバルレースの生存率**
>100人で始まったレースで、第1チェックポイントで5人脱落したら生存率は $95/100$ 。第2チェックポイントで残った95人のうち10人脱落したら、その時点の生存率は $95/100 \times 85/95$ 。このように、各イベント発生時に「その時の残り人数に対して何割生き残ったか」を次々に掛け算してグラフにします。数式（分布）を仮定しない「ノンパラメトリック」な手法なので、どんなデータにも使えます。

### 19.7 Cox比例ハザードモデル
イベントの発生に影響を与えることが想定される共変量を$x_1，\cdots，x_n$として，次のようにハザード関数を表すモデル。
$$
\begin{aligned}
h(t; \boldsymbol{x}) &= h_0(t) \exp(\boldsymbol{x}^\top \boldsymbol{\beta}) \\
&= h_0(t) \exp(\beta_1 x_1 + \beta_2 x_2 + \dots + \beta_n x_n)
\end{aligned}
$$
$h_0(t)$は基準ハザード（ベースラインハザード）と呼ばれる正の値をとる$t$の関数で，特定の分布形を仮定しません。Cox比例ハザードモデルをあてはめる場合には，**比例ハザード性**（ハザード比が時間によって変化しない）を確認する必要があります。

*   **何これ：** 生存時間の長さを、複数の原因（年齢、薬の有無など）で説明する最強モデル
>[!tip] **たとえ話：リスクの掛け算（平行移動）**
>「薬を飲んでいるグループ」のリスクが「飲んでいないグループ」の常に0.5倍である、というように、グループ間のリスクの「比率」が時間によらず一定だと仮定します。生存関数の2重対数プロットが平行になるかどうかで、この「比例ハザード性」という前提が正しいかをチェックします。

### 19.8 ニューラルネットワークモデル
$d$個の入力$x_1，x_2，\cdots，x_d$にそれぞれの重み$w_1，w_2，\cdots，w_d$をかけ，これらの積の合計にバイアス$b$を加えた$\boldsymbol{w}^T\boldsymbol{x}＋b＝b＋w_1x_1＋\cdots＋w_dx_d$の入力を受けた1つのユニットは，活性化関数$f$を施して$f(\boldsymbol{w}^T\boldsymbol{x}＋b)$を出力します。このようなユニットを層状に並べ，層自体も複数並べたものがニューラルネットワークモデルです。各ユニットに非線形活性化関数を使用することで，複雑な非線形関係をモデル化することが可能になります。

*   **何これ：** 脳の神経回路をマネして、どんな複雑な「グニャグニャの図形」でも近似するモデル
>[!tip] **たとえ話：情報のバケツリレー**
>各ユニットは、たくさんの友人（入力）から話を聞きますが、それぞれの友人の信頼度（重み）を変えて聞き取り、最後に自分の意見（活性化関数）を乗せて次の層へ伝えます。この「重み」をデータから学習することで、人間には見抜けないような複雑なパターンをコンピュータが発見できるようになります。